/**
 * Keyword: variable
 */
var user1 = "User";
var user2 = "User";

/**
 * Keyword: interface
 */
interface Person {
	firstName: string;
	lastName: string;
}

/**
 * Keyword: function
 */
function add(x, y) {
	return x + y;
}
